<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="prestamo.list">
      <field name="name">prestamo list</field>
      <field name="model">prestamo.prestamo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="prestamo.action_window">
      <field name="name">prestamo window</field>
      <field name="res_model">prestamo.prestamo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="prestamo.action_server">
      <field name="name">prestamo server</field>
      <field name="model_id" ref="model_prestamo_prestamo"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <menuitem name="prestamo" id="prestamo.menu_root"/>
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="prestamo.menu_1" parent="prestamo.menu_root"/>
    <menuitem name="Menu 2" id="prestamo.menu_2" parent="prestamo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="prestamo.menu_1_list" parent="prestamo.menu_1"
              action="prestamo.action_window"/>
    <menuitem name="Server to list" id="prestamo" parent="prestamo.menu_2"
              action="prestamo.action_server"/>
    -->
  </data>
</odoo>
